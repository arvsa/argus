# Swarm stack: one argus-client zone (deploy with scripts/swarm/deploy.sh
# client <#>). Same swarm-isms as stack.server.yml -- see the header there
# and plan/swarm-orchestration-v1.md. Every numbered client stack gets its
# own db/redis/volumes (swarm prefixes them with the stack name), so
# argus-client-1 and argus-client-2 are fully independent zones.

services:

  db:
    image: mysql
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost', '-u', 'root', '-p${MYSQL_ROOT_PASSWORD}']
      interval: 10s
      retries: 5
      start_period: 30s
      timeout: 10s
    volumes:
      - app-db-data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD?Variable not set}
      - MYSQL_DATABASE=${MYSQL_DATABASE?Variable not set}

  redis:
    image: redis:8-alpine
    volumes:
      - redis-data:/data

  prestart:
    image: '${DOCKER_IMAGE_BACKEND?Variable not set}:${TAG:-latest}'
    command: bash scripts/prestart.sh
    deploy:
      restart_policy:
        condition: none
    environment:
      - DOMAIN=${DOMAIN}
      - FRONTEND_HOST=${FRONTEND_HOST?Variable not set}
      - ENVIRONMENT=${ENVIRONMENT}
      - BACKEND_CORS_ORIGINS=${BACKEND_CORS_ORIGINS}
      - SECRET_KEY=${SECRET_KEY?Variable not set}
      - FIRST_SUPERUSER=${FIRST_SUPERUSER?Variable not set}
      - FIRST_SUPERUSER_PASSWORD=${FIRST_SUPERUSER_PASSWORD?Variable not set}
      - PROJECT_NAME=${PROJECT_NAME:-argus}
      - MYSQL_SERVER=db
      - MYSQL_PORT=${MYSQL_PORT:-3306}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD?Variable not set}
      - SENTRY_DSN=${SENTRY_DSN:-}
    volumes:
      - ${ARGUS_REPO_DIR?Variable not set}/hierarchy.yaml:/app/backend/hierarchy.yaml:ro

  backend:
    image: '${DOCKER_IMAGE_BACKEND?Variable not set}:${TAG:-latest}'
    networks:
      - traefik-public
      - default
    environment:
      - DOMAIN=${DOMAIN}
      - FRONTEND_HOST=${FRONTEND_HOST?Variable not set}
      - ENVIRONMENT=${ENVIRONMENT}
      - BACKEND_CORS_ORIGINS=${BACKEND_CORS_ORIGINS}
      - SECRET_KEY=${SECRET_KEY?Variable not set}
      - FIRST_SUPERUSER=${FIRST_SUPERUSER?Variable not set}
      - FIRST_SUPERUSER_PASSWORD=${FIRST_SUPERUSER_PASSWORD?Variable not set}
      - PROJECT_NAME=${PROJECT_NAME:-argus}
      - MYSQL_SERVER=db
      - MYSQL_PORT=${MYSQL_PORT:-3306}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD?Variable not set}
      - SENTRY_DSN=${SENTRY_DSN:-}
      - REDIS_URL=redis://redis:6379/0
      - ROLE=client
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/utils/health-check/"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      labels:
        - traefik.enable=true
        - traefik.constraint-label=traefik-public
        - traefik.http.services.${STACK_NAME?Variable not set}-backend.loadbalancer.server.port=8000
        - traefik.http.routers.${STACK_NAME?Variable not set}-backend-http.rule=Host(`api.${DOMAIN?Variable not set}`)
        - traefik.http.routers.${STACK_NAME?Variable not set}-backend-http.entrypoints=http
        - traefik.http.routers.${STACK_NAME?Variable not set}-backend-http.middlewares=https-redirect
        - traefik.http.routers.${STACK_NAME?Variable not set}-backend-https.rule=Host(`api.${DOMAIN?Variable not set}`)
        - traefik.http.routers.${STACK_NAME?Variable not set}-backend-https.entrypoints=https
        - traefik.http.routers.${STACK_NAME?Variable not set}-backend-https.tls=true
        - traefik.http.routers.${STACK_NAME?Variable not set}-backend-https.tls.certresolver=le

  frontend:
    image: '${DOCKER_IMAGE_FRONTEND?Variable not set}:${TAG:-latest}'
    networks:
      - traefik-public
      - default
    environment:
      # Every zone's frontend shares the same external traefik-public
      # overlay network, so the bare Compose DNS name "backend" is
      # ambiguous across stacks -- see frontend/nginx.conf.template.
      # "<stack-name>_backend" is swarm's own globally-unique full service
      # name, unambiguous regardless of shared networks.
      - BACKEND_UPSTREAM=${STACK_NAME?Variable not set}_backend
    deploy:
      labels:
        - traefik.enable=true
        - traefik.constraint-label=traefik-public
        - traefik.http.services.${STACK_NAME?Variable not set}-frontend.loadbalancer.server.port=80
        - traefik.http.routers.${STACK_NAME?Variable not set}-frontend-http.rule=Host(`dashboard.${DOMAIN?Variable not set}`)
        - traefik.http.routers.${STACK_NAME?Variable not set}-frontend-http.entrypoints=http
        - traefik.http.routers.${STACK_NAME?Variable not set}-frontend-http.middlewares=https-redirect
        - traefik.http.routers.${STACK_NAME?Variable not set}-frontend-https.rule=Host(`dashboard.${DOMAIN?Variable not set}`)
        - traefik.http.routers.${STACK_NAME?Variable not set}-frontend-https.entrypoints=https
        - traefik.http.routers.${STACK_NAME?Variable not set}-frontend-https.tls=true
        - traefik.http.routers.${STACK_NAME?Variable not set}-frontend-https.tls.certresolver=le

  pingsvc:
    image: '${DOCKER_IMAGE_PINGSVC?Variable not set}:${TAG:-latest}'
    networks:
      # traefik-public only so it can reach the shared dev MinIO's `minio`
      # alias; it serves nothing through Traefik (metrics stay stack-local
      # on :9090 -- publishing them would collide across numbered stacks).
      - traefik-public
      - default
    environment:
      - REDIS_URL=redis:6379
      - PINGSVC_CONCURRENCY=${PINGSVC_CONCURRENCY:-200}
      - PINGSVC_GLOBAL_QPS=${PINGSVC_GLOBAL_QPS:-500}
      - PINGSVC_PER_HOST_TIMEOUT=${PINGSVC_PER_HOST_TIMEOUT:-2s}
      - ARGUS_ROLE=${ARGUS_ROLE:-both}
      - ARGUS_ZONE_ID=${ARGUS_ZONE_ID?Variable not set}
      - ARGUS_TENANT_ID=${ARGUS_TENANT_ID:-default}
      - ARGUS_SPOOL_DIR=${ARGUS_SPOOL_DIR:-/var/lib/argus/pending}
      - ARGUS_S3_BUCKET=${ARGUS_S3_BUCKET:-}
      - ARGUS_S3_REGION=${ARGUS_S3_REGION:-us-east-1}
      - ARGUS_S3_ENDPOINT=${ARGUS_S3_ENDPOINT:-}
      - ARGUS_S3_ACCESS_KEY=${ARGUS_S3_ACCESS_KEY:-}
      - ARGUS_S3_SECRET_KEY=${ARGUS_S3_SECRET_KEY:-}
      - ARGUS_SIGNING_KEY_PATH=${ARGUS_SIGNING_KEY_PATH:-/var/lib/argus/signing.key}
    volumes:
      # Single-node bind mount, one shared targets file per host by default;
      # point ARGUS_TARGETS_FILE at a per-stack copy to differ per zone.
      - ${ARGUS_TARGETS_FILE?Variable not set}:/etc/pingsvc/targets.txt:ro
      # Spool dir AND the Ed25519 signing key -- stack-name-prefixed volume,
      # so each numbered zone keeps its own persistent identity.
      - argus-data:/var/lib/argus
    cap_add:
      - NET_RAW
      - NET_ADMIN
    deploy:
      resources:
        limits:
          memory: 512M

volumes:
  app-db-data:
  redis-data:
  argus-data:

networks:
  traefik-public:
    external: true
